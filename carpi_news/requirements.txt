# Django and core dependencies
Django==5.2.5
python-dotenv==1.0.0

# Database adapters
dj-database-url==2.1.0
psycopg2-binary==2.9.9  # For PostgreSQL (optional)

# Web scraping and monitoring
requests==2.31.0
beautifulsoup4==4.12.2
PyPDF2==3.0.1
pdfplumber==0.11.0
Pillow==10.4.0

# AI Integration
anthropic==0.64.0

# Scheduling
schedule==1.2.1
psutil==5.9.8  # For cross-platform process checking

# YouTube integration
youtube-transcript-api>=0.7.0

# Social media integration
# Note: Facebook/Twitter now handled via RSS + IFTTT
# Only Telegram Bot API used directly (via requests)

# Web server (for production)
gunicorn==21.2.0
whitenoise==6.6.0

# RSS feed generation (built into Django)
# No additional dependencies needed for RSS feeds

# Environment and utilities
python-decouple==3.8  # Alternative to python-dotenv
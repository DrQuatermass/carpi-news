<?xml version="1.0" encoding="UTF-8"?>
<sitemapindex xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <!-- Sitemap principale con articoli recenti (ultimi 30 giorni) -->
  <sitemap>
    <loc>https://ombradelportico.it/sitemap.xml</loc>
    <lastmod>{{ main_lastmod|date:"c" }}</lastmod>
  </sitemap>

  <!-- Sitemap Google News (ultimi 2 giorni) -->
  <sitemap>
    <loc>https://ombradelportico.it/sitemap-news.xml</loc>
    <lastmod>{{ news_lastmod|date:"c" }}</lastmod>
  </sitemap>

  <!-- Sitemap archivio per articoli più vecchi (se necessario) -->
  {% if has_archive %}
  <sitemap>
    <loc>https://ombradelportico.it/sitemap-archive.xml</loc>
    <lastmod>{{ archive_lastmod|date:"c" }}</lastmod>
  </sitemap>
  {% endif %}
</sitemapindex>
